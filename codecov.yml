coverage:
  status:
    project:
      default:
        target: 80%
    patch:
      default:
        target: 80%

ignore:
  - "**/*.tsx"
  - "src/use-app-form.ts"

flags:
  unit:
    paths:
      - src/
    carryforward: true